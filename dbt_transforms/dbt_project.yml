name: 'qcommerce_transforms'
version: '1.0.0'
config-version: 2

profile: 'qcommerce_profile'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  qcommerce_transforms:
    staging:
      +materialized: view
    marts:
      +materialized: table