qcommerce_profile:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: de_user
      password: de_password
      port: 5432
      dbname: warehouse
      schema: analytics
      threads: 4